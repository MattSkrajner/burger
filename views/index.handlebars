<div class="container">
    <h1>Eat-Da-Burger!</h1>

    <h2>It's Burger Time!</h2>

    {{#each data}}
        {{#unless this.devoured}}
            
            <div class="col-xs-9 text-center">
                <pre>{{this.id}}, {{this.burger_name}}</pre>
            </div>

            <div class="col-xs-3 text-center">
                <form method="PUT" action="/burgers/updateOne?_method=PUT" class="button-size">
                    <input type="hidden" name="burger_id" type="text" value={{this.id}}>
                    <br>
                    <button type="submit" class="btn btn-default">Devour the Burger!</button>
                </form>
            </div>

        {{/unless}}
    {{/each}}


    <div class="col-xs-6 text-center" class="task">
        
        {{#each data}}
            {{#if this.devoured}}
            
                <input class="form-control" type="text" placeholder="{{this.id}}, {{this.burger_name}}" readonly>
        
            {{/if}}
        {{/each}}
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-xs-12 text-center enter_section">
            <form action="/burgers/create" method="POST">
                <input type="text" name="burger_name" class="form-control" value="" id="enter_text">
                <button type="submit" class="btn btn-primary" id="text-enter-button">Click to Add a New Burger!</button>
            </form>
        </div>
    </div>
</div>


{{!-- <h2>Add a New Burger. Use Your Imagination!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Add The Burger</button>
</form> --}}
